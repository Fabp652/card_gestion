{% for item in items %}
    <li>
        <a
            class="d-block p-2 link-underline link-underline-opacity-0 text-black item-list"
            href="{{ path('app_item_view', {id: item.id}) }}"
            
        >
            {{ item.choiceLabel }}
        </a>
    </li>
{% endfor %}