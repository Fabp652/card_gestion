{{ form_start(form, {method: 'POST', action: path('app_item_sale_new'), attr: {'id': 'modalForm', 'data-controller': 'select'}}) }}
    <div class="row">
        <div class="col-12 mb-3">
            {{ form_row(form.name) }}
        </div>
        <div class="col-12 mb-3">
            {{ form_row(form.price) }}
        </div>
        <div class="col-12 mb-3">
            {{ form_row(form.link) }}
        </div>
        <div class="col-12 mb-3">
            {{ form_row(form.itemQualities) }}
        </div>
    </div>
{{ form_end(form) }}