<ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
    {% for category in categories %}
        <li class="nav-item">
            <a class="nav-link" href="{{ absolute_url(path('app_item_list', {collectionId: collectionId, categoryId: category.id}))}}">{{ category.name }}</a>
        </li>
    {% endfor %}
</ul>